name: test
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  # required
  - xarray
  - pydap
  - lxml
  - requests
  - dask
  - netCDF4
  # testing
  - pytest
  - pytest-cov
  - pip:
    - codecov
