name: test
channels:
  - defaults
dependencies:
  - python=3.5
  # required
  - xarray
  - pydap
  - lxml
  - requests
  - dask
  - netCDF4
  # testing
  - pytest
  - pytest-cov
  - pip:
    - codecov
