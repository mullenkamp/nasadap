name: test
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.5
  # required
  - xarray
  - pydap
  - lxml
  - requests
  - dask
  - netCDF4
  - xmltodict
  # testing
  - pytest
  - pytest-cov
  - pip:
    - codecov
